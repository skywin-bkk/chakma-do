const departments=Array.from({length:14},(_,i)=>`DO-DEP-${String(i+1).padStart(2,'0')}`);
const grid=document.querySelector('#deptGrid');if(grid){departments.forEach(id=>{const a=document.createElement('article');a.innerHTML=`<small>${id}</small><b>Department</b><p>Public profile pending authoritative title verification.</p>`;grid.appendChild(a)});}
const menu=document.querySelector('#menu'),nav=document.querySelector('#nav');if(menu&&nav){menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));}
